# Environment Configuration
NODE_ENV=production

# Web3Forms Configuration (Primary Email Service)
WEB3FORMS_ACCESS_KEY=badf4ca6-e440-43be-abbf-e0e6c4b7663b

# Gmail Configuration (Optional - for Gmail delivery method)
# To enable Gmail, you need to set up OAuth2 credentials:
# 1. Go to Google Cloud Console
# 2. Create a project and enable Gmail API
# 3. Create OAuth2 credentials
# 4. Use OAuth2 playground to get refresh token
VITE_GMAIL_CLIENT_ID=your_gmail_client_id_here
VITE_GMAIL_CLIENT_SECRET=your_gmail_client_secret_here
VITE_GMAIL_REFRESH_TOKEN=your_gmail_refresh_token_here

# Port Configuration
PORT=3001

# Instructions for Gmail Setup:
# 1. Visit: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Gmail API
# 4. Go to Credentials > Create Credentials > OAuth 2.0 Client IDs
# 5. Set application type to "Web application"
# 6. Add authorized redirect URI: https://developers.google.com/oauthplayground
# 7. Copy Client ID and Client Secret
# 8. Visit: https://developers.google.com/oauthplayground
# 9. In settings, use your own OAuth credentials
# 10. Authorize Gmail API v1 scope: https://www.googleapis.com/auth/gmail.send
# 11. Exchange authorization code for tokens
# 12. Copy the refresh token